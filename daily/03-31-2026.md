```mysql
SELECT
    HouseState, COUNT(*)
FROM
    HOUSE
GROUP BY HouseState;
```


```mysql
SELECT
    HouseBuildYear, COUNT(*)
FROM
    HOUSE
GROUP BY HouseBuildYear;
```

```mysql
SELECT
    HouseState, HouseBuildYear, COUNT(*)
FROM
    HOUSE
GROUP BY HouseBuildYear, HouseState WITH ROLLUP;
```


```mysql
SELECT
    ChannelID, COUNT(*)
FROM
    STAY
GROUP BY ChannelID;
```


```mysql
SELECT
    ChannelID AS Channel,
    MONTH(StayStartDate) AS Month,
    COUNT(*) AS Total
FROM
    STAY
GROUP BY ChannelID , MONTH(StayStartDate) WITH ROLLUP;
```


```mysql
SELECT
    HouseID, MONTH(StayStartDate), COUNT(*)
FROM
    STAY
GROUP BY HouseID, MONTH(StayStartDate) WITH ROLLUP
ORDER BY HouseID, MONTH(StayStartDate);
```


```mysql
SELECT
    GuestID, MONTH(StayStartDate), COUNT(*)
FROM
    STAY
WHERE
    MONTH(StayStartDate ) > 6
GROUP BY GuestID, MONTH(StayStartDate) WITH ROLLUP;
```


```mysql
SELECT 
    GuestID, MONTH(StayStartDate), COUNT(*)
FROM
    STAY
WHERE
    MONTH(StayStartDate ) > 6
GROUP BY GuestID, MONTH(StayStartDate) WITH ROLLUP;
```


```

SELECT

    CAR_MODEL, COUNT(*)

FROM

    CARS

GROUP BY CAR_MODEL;
```


```


SELECT

    CAR_MODEL, CAR_YEAR, COUNT(*)

FROM

    CARS

GROUP BY CAR_YEAR, CAR_MODEL WITH ROLLUP;
```



Programming master, tetris master, SQL master, javascript master, league of legends master, elden ring master, parenting master, cooking master, fortnite master, workout master, weed smoking master, no wonder im overloaded but im thinking what is next


Drivers and their car useage
In the month October
Each driver and allocation occured

DRIVER_ID and CAR_ID with their usage counts as the TOTAL column for the travels occured in the month of OCTOBER


total nmber of ravels by the driveres you need to groupby rollup


Application is due tomorrow
- only cover books, tuition, and fees

